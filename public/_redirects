# Cloudflare Pages' native SPA fallback serves index.html for localized routes.
# Explicit rewrites to /index.html are intentionally omitted because Pages'
# pretty-URL canonicalization would turn them into redirect loops.
